# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# local editor / tooling helpers
/.claude/

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# typescript
*.tsbuildinfo

# ---- secrets & real data (NEVER commit — this repo is public) ----
.env
.env.local
.env*.local

# Working board config may contain real org/repo names — keep it local.
/data/board.json
# Disposable live-sync cache (may hold real milestone/issue data).
/data/synced.json

# Local fixture overlay for simulating a private board offline (e.g. real repo
# names). Merged over the committed fictional fixtures by the MockProvider.
/fixtures/*.local.json

# Visual brainstorming companion scratch
/.superpowers/
